<div class="container">
  <h3 class="text-center" style="padding-top: 30px;">Create new board</h3>
  <%= simple_form_for(@board) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.submit "Create board", class: "btn btn-primary" %>
  <% end %>
</div>
