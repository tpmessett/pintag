<div class="container">
<div class="col-sm-6 offset-sm-3">
<div class="container2 p-5">
  <h3 class="text-center" style="padding-top: 30px;">Edit <%= @board.name %></h3>
  <%= simple_form_for(@board) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.submit "Update board", class: "btn btn-primary" %>
  <%= link_to "Delete board", board_path(@board), method: :delete, class: "deletebutton btn",data: {Confirm: "delete board?"} %>
 <% end %>
 
  </div>
  </div>
</div>
 <br>
 <br>