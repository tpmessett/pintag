<div class="container">
  <h3 class="text-center" style="padding-top: 30px;">Create new tag</h3>
  <%= simple_form_for(@tag) do |f| %>
  <%= f.input :name %>
  <%= f.submit "Create tag", class: "btn btn-primary" %>
  <% end %>
</div>
